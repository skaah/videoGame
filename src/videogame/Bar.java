package videogame;

public abstract class Bar extends Character {
	
	public Bar(String name) {
		super(name);
		// TODO Auto-generated constructor stub
	}
//	protected Float manaBar = new Float(getMana());
//	protected Float energyBar = new Float(getEnergy());
//	protected Float rageBar = new Float(getRage() - ((2 / 100) * getRage()));

}
